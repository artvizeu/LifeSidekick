"use strict";

lifeSidekickApp
    .controller('AboutCtrl', function($rootScope, $scope, $ionicViewService) {
        // set the rate and max variables
        $scope.rate = 3;
        $scope.max = 5;

    });